Use previews, filenames, and metadata to infer document purpose and content type.
If the file is not text-readable, base your analysis on the filename and MIME type.
Respond with ONLY valid JSON matching the schema — no prose, no explanation.
