{
  "name": "@micro-harnesses/content-analysis",
  "version": "0.1.0",
  "private": true,
  "description": "Web/API app for multimodal content analysis using @micro-harnesses/core.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@micro-harnesses/core": "^3.3.1",
    "pdf-parse": "^1.1.1"
  },
  "devDependencies": {
    "@types/node": "^22.20.0",
    "@types/pdf-parse": "^1.1.4"
  }
}
