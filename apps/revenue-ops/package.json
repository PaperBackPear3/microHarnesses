{
  "name": "@micro-harnesses/revenue-ops",
  "version": "0.1.0",
  "private": true,
  "description": "Revenue retention + collections automation app using @micro-harnesses/core.",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.json",
    "test": "node --test \"dist/**/*.test.js\""
  },
  "dependencies": {
    "@micro-harnesses/core": "^3.4.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.0"
  }
}
