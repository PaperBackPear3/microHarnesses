{
  "name": "micro-harnesses",
  "version": "0.2.0",
  "private": true,
  "description": "Library-first framework for building micro harnesses.",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspace @micro-harness/core",
    "start": "npm run build --silent && node apps/cli/dist/index.js",
    "cli:run": "npm run build --silent && node apps/cli/dist/index.js run",
    "checkpoints:list": "npm run build --silent && node apps/cli/dist/index.js checkpoints list"
  },
  "devDependencies": {}
}
