# Claude Code instructions

Use [`.github/copilot-instructions.md`](.github/copilot-instructions.md) as the
canonical repository agent guide. Keep this file as a pointer instead of
duplicating the full instructions.